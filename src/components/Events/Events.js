import React from 'react';
import Header from '../Shared/Header/Header';

const Events = () => {
    return (
        <div>
            <Header></Header>
            <h2>Events</h2>
        </div>
    );
};

export default Events;